<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Application Version
    |--------------------------------------------------------------------------
    |
    | Bump this whenever a new feature is deployed so the running version can
    | be verified in the UI (shown in the footer / settings page).
    |
    */
    'version' => '1.1.0',

    /*
    |--------------------------------------------------------------------------
    | Default Reading Goals
    |--------------------------------------------------------------------------
    |
    | Fallback goals used when the admin has not configured custom goals in the
    | Settings screen. Pages per day and per month.
    |
    */
    'default_daily_goal_pages' => 5,
    'default_monthly_goal_pages' => 150,

    /*
    |--------------------------------------------------------------------------
    | Quran Constants
    |--------------------------------------------------------------------------
    */
    'total_pages' => 604,   // Pages in the standard Madani Mushaf
    'total_juz' => 30,
    'total_surahs' => 114,

];
